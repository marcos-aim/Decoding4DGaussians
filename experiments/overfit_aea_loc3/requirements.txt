# Per-experiment pinned deps for overfit_aea_loc3 (Aria vs 4DGT comparison).
#
# Use this to build an ISOLATED env on the cluster — do NOT install into the
# shared Decode4DGS env. The shared env is known-incompatible here:
#   * numpy 2.x          -> projectaria-tools<2 needs numpy<2
#   * transformers 5.x   -> VGGT4Track.from_pretrained API broke in 5.x
#   * missing            -> flash-attn, projectaria-tools, gsplat
#
# Recommended setup (per-user, doesn't touch anyone else):
#
#   conda create -n aria-compare python=3.10 -y
#   conda activate aria-compare
#   conda install -c nvidia -c conda-forge cuda-toolkit=12.4 -y
#   pip install -r experiments/overfit_aea_loc3/requirements.txt
#   pip install --no-build-isolation ./4DGaussians/submodules/depth-diff-gaussian-rasterization
#   pip install --no-build-isolation ./4DGaussians/submodules/simple-knn
#
# Torch wheels come from PyTorch's cu124 index; leave this extra-index-url line in place.
--extra-index-url https://download.pytorch.org/whl/cu124

# --- core ---
torch==2.4.1+cu124
torchvision==0.19.1+cu124
numpy<2.0

# --- attention kernels (VGGT/STV2 backbone) ---
xformers==0.0.28
flash-attn==2.6.3

# --- Aria dataset tooling ---
projectaria-tools[all]==1.5.5

# --- rasterizer deps & baselines ---
gsplat==1.4.0
open3d==0.18.0
rerun-sdk==0.20.0

# --- metrics ---
lpips==0.1.4
pytorch-msssim==1.0.0

# --- configs / hydra / loaders ---
omegaconf==2.3.0
hydra-core==1.3.2
pyyaml
tqdm
tensorboard

# --- image I/O ---
opencv-python-headless==4.10.0.84
imageio[ffmpeg]==2.36.0

# --- HF stack pinned to VGGT4Track-compatible versions ---
huggingface_hub==0.25.0
transformers==4.45.0
safetensors
einops
